@page "/Branches"
@rendermode InteractiveServer

<PageTitle>Branches | Vex</PageTitle>

<Tabs
    SelectedTab="@selectedTab"
    SelectedTabChanged="@OnSelectedTabChanged"
    Justified="true"
    RenderMode="TabsRenderMode.LazyLoad"
>
    <Items>
        <Tab Name="Create">Create</Tab>
        <Tab Name="Branches">Branches</Tab>
        <Tab Name="Analytics">Analytics</Tab>
    </Items>
    <Content>
        <TabPanel Name="Create">
            <BranchesCreate />
        </TabPanel>
        <TabPanel Name="Branches">
            <BranchesGrid />
        </TabPanel>
        <TabPanel Name="Analytics">
            <BranchesAnalytics />
        </TabPanel>
    </Content>
</Tabs>

@code{
    string selectedTab = "Create";

    private Task OnSelectedTabChanged( string name )
    {
        selectedTab = name;
        return Task.CompletedTask;
    }
}