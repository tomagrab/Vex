@page "/Tickets"
@rendermode InteractiveServer

<PageTitle>Tickets | Vex</PageTitle>

<Tabs
    SelectedTab="@selectedTab"
    SelectedTabChanged="@OnSelectedTabChanged"
    Justified="true"
    RenderMode="TabsRenderMode.LazyLoad"
>
    <Items>
        <Tab Name="Create">Create</Tab>
        <Tab Name="Tickets">Tickets</Tab>
        <Tab Name="Analytics">Analytics</Tab>
    </Items>
    <Content>
        <TabPanel Name="Create">
            <TicketsCreate />
        </TabPanel>
        <TabPanel Name="Tickets">
            <TicketsGrid />
        </TabPanel>
        <TabPanel Name="Analytics">
            <TicketsAnalytics />
        </TabPanel>
    </Content>
</Tabs>

@code{
    string selectedTab = "Create";

    private Task OnSelectedTabChanged( string name )
    {
        selectedTab = name;
        return Task.CompletedTask;
    }
}